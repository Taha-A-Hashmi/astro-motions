{
  "name": "astro-motions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Astro Motions — immersive WebGL site. Websites with their own gravity.",
  "scripts": {
    "dev": "vite",
    "dev:all": "node scripts/dev.mjs",
    "server": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --disable-warning=ExperimentalWarning --watch server/index.js",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node --env-file-if-exists=.env --env-file-if-exists=.env.local --disable-warning=ExperimentalWarning server/index.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "express": "^5.2.1",
    "gsap": "^3.15.0",
    "lenis": "^1.3.26",
    "nodemailer": "^9.0.6",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  },
  "engines": {
    "node": ">=22.13"
  }
}
